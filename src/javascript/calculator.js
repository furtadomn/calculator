function clean() {
    let C = window.document.querySelector('input.display').value=""
}

function button(number) {
    let B = window.document.querySelector('input.display')
    B.value = B.value + number
}
